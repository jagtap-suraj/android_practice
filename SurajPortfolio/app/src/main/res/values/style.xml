<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/primary</item>
        <item name="colorPrimaryDark">@color/primaryDark</item>
        <item name="colorAccent">@color/accent</item>
        <item name="android:windowTranslucentStatus">true</item> <!-- Make status bar transparent -->
        <item name="android:windowActionBar">false</item> <!-- Remove action bar -->
        <item name="android:windowNoTitle">true</item> <!-- Remove title -->
    </style>

    <style name="RoundedButton">
        <item name="android:background">@drawable/button_rounded_bg</item>
        <item name="android:textColor">@color/white</item> <!-- Set text color as per your design -->
        <item name="android:textAllCaps">false</item> <!-- Optionally, set text to not all caps -->
        <item name="android:layout_width">match_parent</item> <!-- Set width to match_parent -->
        <item name="android:layout_height">wrap_content</item> <!-- Set height to wrap_content -->
        <!-- You can add more attributes as per your requirement -->
    </style>

    <style name="RoundedDialog">
        <item name="android:background">@drawable/dialog_rounded_bg</item>
        <item name="android:textColor">@color/black</item> <!-- Set text color as per your design -->
        <item name="android:textAllCaps">false</item> <!-- Optionally, set text to not all caps -->
        <item name="android:layout_width">match_parent</item> <!-- Set width to match_parent -->
        <item name="android:layout_height">wrap_content</item> <!-- Set height to wrap_content -->
        <!-- You can add more attributes as per your requirement -->
    </style>

    <style name="DialogTheme" parent="Theme.AppCompat.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowAnimationStyle">@style/DialogAnimation</item>
    </style>

    <style name="DialogAnimation">
        <item name="android:windowEnterAnimation">@android:anim/fade_in</item>
        <item name="android:windowExitAnimation">@android:anim/fade_out</item>
    </style>



    <style name="ShapeAppearanceOverlay.App.CircularImageView" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>


</resources>